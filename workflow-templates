name:
on:
  issues:
    types:
      - reopened
      - opened

jobs:
  
     runs-on:
     permissions:
       issues:
     steps:
       - name:
         uses:
         with:
           add-labels:
           repo-token: ${{ secrets.GITHUB_TOKEN }}
